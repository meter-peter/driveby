\chapter{Evaluation}
\label{ch:evaluation}

This chapter evaluates DDT and the DriveBy framework across three dimensions: a controlled experiment against a reference API designed to exercise all nine principles, a large-scale evaluation against real-world public APIs from the APIs.guru registry~\cite{apis-guru}, and an operational evaluation of the XSDLC quality gate system deployed in production on Novelcore's internal development platform. The chapter concludes with CI/CD integration metrics that quantify the system's footprint and performance characteristics.

% ============================================================================
\section{Evaluation Framework}
\label{sec:eval-framework}
% ============================================================================

The evaluation addresses the three research questions from Chapter~\ref{ch:introduction}:

\begin{description}
    \item[RQ1] (Methodology soundness): Does DDT provide automated quality assurance comparable to manual approaches? Evaluated via the controlled experiment (Section~\ref{sec:controlled-eval}).
    \item[RQ2] (Automation extent): To what extent can validation and testing be derived from specifications without manual test authorship? Evaluated via the large-scale experiment (Section~\ref{sec:wild-eval}) and operational deployment (Section~\ref{sec:operational-eval}).
    \item[RQ3] (GitOps integration): How effectively does DDT integrate into cloud-native pipelines as an autonomous quality gate? Evaluated via the production deployment on Novelcore's cluster (Section~\ref{sec:operational-eval}).
\end{description}

% ============================================================================
\section{Controlled Evaluation: perfect-api}
\label{sec:controlled-eval}
% ============================================================================

The controlled evaluation uses a reference API (\texttt{perfect-api}) designed to satisfy all nine DDT principles at baseline, then systematically injects defects to verify detection accuracy.

% ----------------------------------------------------------------------------
\subsection{Reference API Design}
\label{subsec:reference-api}
% ----------------------------------------------------------------------------

The \texttt{perfect-api} is a Python FastAPI application with a hand-crafted OpenAPI specification that satisfies every check of every principle. It defines eight operations across three resource groups: five CRUD endpoints for \texttt{/products} (list, create, get by ID, update, delete), a \texttt{POST /tasks} endpoint, and two utility endpoints (\texttt{GET /test/health}, \texttt{POST /test/echo}). The specification includes comprehensive error responses (400, 401, 403, 404, 500) with consistent schemas, typed parameters with constraints, OAuth2 security with scopes, semantic versioning with migration documentation, and response examples for all 2xx responses. The API is deployed to the \texttt{perfect-api-dev} namespace on \texttt{private.novelcore.org} and serves as the validation target for all three evaluation arms.

% ----------------------------------------------------------------------------
\subsection{Baseline Validation}
\label{subsec:baseline}
% ----------------------------------------------------------------------------

At baseline, \texttt{perfect-api} passes all seven implemented principle checkers across all validation modes:

\begin{table}[htbp]
\centering
\caption{Baseline validation results for \texttt{perfect-api} across validation modes.}
\label{tab:baseline-results}
\small
\begin{tabular}{@{}lccccccccl@{}}
\toprule
\textbf{Mode} & \textbf{P001} & \textbf{P002} & \textbf{P003} & \textbf{P004} & \textbf{P005} & \textbf{P008} & \textbf{P009} & \textbf{Result} \\
\midrule
Minimal    & \cmark & ---    & ---    & ---    & ---    & ---    & ---    & 1/1 passed \\
Test-ready & \cmark & ---    & ---    & \cmark & ---    & ---    & \cmark & 3/3 passed \\
Strict     & \cmark & \cmark & \cmark & \cmark & \cmark & \cmark & ---    & 6/6 passed \\
\bottomrule
\end{tabular}

\noindent\small{``---'' indicates the principle is not evaluated in that mode.}
\end{table}

This baseline establishes the zero-false-positive property: a specification that genuinely satisfies all principles produces no spurious findings. Any finding reported by DriveBy against \texttt{perfect-api} at baseline would constitute a false positive.

% ----------------------------------------------------------------------------
\subsection{Defect Injection and Detection}
\label{subsec:defect-injection}
% ----------------------------------------------------------------------------

To verify detection accuracy, seven defects are injected into the \texttt{perfect-api} specification, one targeting each implemented principle. Each defect is designed to violate exactly one principle while leaving others unaffected:

\begin{table}[htbp]
\centering
\caption{Defect injection matrix: each injected defect targets one principle.}
\label{tab:defect-injection}
\small
\begin{tabular}{@{}llp{7cm}c@{}}
\toprule
\textbf{Target} & \textbf{Defect} & \textbf{Description} & \textbf{Detected} \\
\midrule
P001 & Duplicate operationId & Two endpoints share the same \texttt{operationId} & \cmark \\
P002 & Missing descriptions  & Remove all operation summaries and descriptions & \cmark \\
P003 & Missing error responses & Remove all 4xx/5xx response definitions & \cmark \\
P004 & Untyped schemas       & Remove type specifications from request body schemas & \cmark \\
P005 & Missing security      & Remove \texttt{components.securitySchemes} entirely & \cmark \\
P008 & Invalid version       & Change \texttt{info.version} to ``latest'' (non-semver) & \cmark \\
P009 & Missing examples      & Remove all parameter examples and schema types & \cmark \\
\bottomrule
\end{tabular}
\end{table}

All seven injected defects are detected with zero false negatives and zero false positives (no unrelated principles are affected by a single-principle defect). The detection is deterministic: running the same validation multiple times against the same defective specification produces identical results, confirming the Axiom of Determinism.

% ============================================================================
\section{Large-Scale Evaluation: APIs.guru}
\label{sec:wild-eval}
% ============================================================================

The large-scale evaluation applies DDT validation to real-world API specifications from the APIs.guru registry~\cite{apis-guru}, assessing how existing APIs perform against DDT's quality standards.

% ----------------------------------------------------------------------------
\subsection{Dataset and Protocol}
\label{subsec:dataset}
% ----------------------------------------------------------------------------

The evaluation harvests OpenAPI specifications from the APIs.guru registry using \texttt{tools/harvest-openapi-apisguru.py}. The dataset includes specifications from major API providers (Stripe, Twilio, GitHub, AWS) as well as smaller independent APIs. Each specification is validated in both \textit{minimal} and \textit{strict} modes using \texttt{tools/run-openapi-batch.sh}. Only static validation principles (P001--P005, P008) are evaluated; P006 (Functional Testing) and P007 (Performance Testing) are excluded because the evaluation does not have authenticated access to the live API endpoints.

The evaluation was conducted through an agent-driven workflow (described in detail in Chapter~\ref{ch:ai-development}): the AI agent developed the harvesting and batch testing tooling, executed the evaluation runs, parsed the structured JSON reports, identified cross-API patterns, and drafted the analysis. The human developer reviewed the interpretation and verified the numbers against raw output. This workflow itself validates a key DDT claim: that the structured, machine-readable output required by the Axiom of Observability is directly consumable by automated actors---including AI agents performing empirical analysis.

% ----------------------------------------------------------------------------
\subsection{Baseline: Swagger Petstore v3}
\label{subsec:petstore-baseline}
% ----------------------------------------------------------------------------

The Swagger Petstore v3---the canonical OpenAPI example maintained by the OpenAPI Initiative---serves as a reference data point. In strict mode, Petstore scores \textbf{1/6}: only P001 (OpenAPI Specification Compliance) passes. The five failing principles reveal systematic documentation quality gaps:

\begin{itemize}
    \item \textbf{P002} (Documentation Quality): Missing operation descriptions, parameter descriptions, and response examples across most endpoints.
    \item \textbf{P003} (Error Handling): No 5xx error responses defined; error response schemas lack structured fields.
    \item \textbf{P004} (Schema Definitions): String parameters lack length constraints; numeric fields lack range constraints.
    \item \textbf{P005} (Security): Security schemes defined but OAuth2 scopes not referenced in security requirements.
    \item \textbf{P008} (Versioning): Version field present and follows semver, but no versioning strategy, deprecation notices, or migration guides documented.
\end{itemize}

This result is significant: \textit{even the canonical example specification maintained by the OpenAPI Initiative fails DDT's strict validation}. This validates the thesis claim that existing specifications systematically neglect documentation quality, error handling completeness, and schema precision---the exact gaps that DDT is designed to detect.

% ----------------------------------------------------------------------------
\subsection{Per-Principle Pass Rates}
\label{subsec:pass-rates}
% ----------------------------------------------------------------------------

Across the APIs.guru dataset, per-principle pass rates reveal a consistent pattern: structural compliance is high, but documentation quality and constraint precision are low.

\begin{table}[htbp]
\centering
\caption{Per-principle pass rates across APIs.guru dataset (strict mode).}
\label{tab:pass-rates}
\small
\begin{tabular}{@{}llcc@{}}
\toprule
\textbf{ID} & \textbf{Principle} & \textbf{Approximate Pass Rate} & \textbf{Severity} \\
\midrule
P001 & OpenAPI Compliance     & High   & Critical \\
P002 & Documentation Quality  & Low    & Warning  \\
P003 & Error Handling         & Low    & Warning  \\
P004 & Schema Definitions     & Low    & Warning  \\
P005 & Security Standards     & Medium & Critical \\
P008 & Versioning Strategy    & Low    & Warning  \\
\bottomrule
\end{tabular}
\end{table}

The gap between P001 (high pass rate) and P002--P004 (low pass rates) confirms that existing tooling validates specification \textit{form} (structural compliance) but not \textit{substance} (documentation completeness, schema precision). DDT's warning-severity principles (P002, P003, P004, P008) target precisely this gap.

% ============================================================================
\section{Operational Evaluation: Production Deployment}
\label{sec:operational-eval}
% ============================================================================

The operational evaluation assesses DDT not as an isolated tool but as a feature of a production internal developer platform (IDP). This section evaluates the system's deployment characteristics, its impact on quality assurance workflow, and its behavior under real operational conditions.

% ----------------------------------------------------------------------------
\subsection{XSDLC as IDP Feature}
\label{subsec:xsdlc-idp}
% ----------------------------------------------------------------------------

The DriveBy quality gate system is deployed on \texttt{private.novelcore.org} as one component of Novelcore's internal developer platform. The IDP is built entirely on Kubernetes operators and Crossplane compositions: database provisioning, networking, observability, and now quality-gated delivery pipelines all follow the same pattern---a custom resource declares the desired state, a composition generates the implementation resources, and Kubernetes controllers reconcile toward that state.

Within this IDP, XSDLC is not a standalone installation. It is a \textit{composable building block} that coexists with other platform capabilities. A single \texttt{XSDLC} custom resource (\(\sim\)25 lines of YAML) provisions approximately 34 Kubernetes resources in under 15 seconds: ArgoCD Applications, WorkflowTemplates, EventSources, Sensors, Ingresses, RBAC roles and bindings, a PromotionStrategy, BranchProtection rules, environment branches, and a generated deploy workflow. Without XSDLC, a platform operator would need to manually create each of these resources through ClickOps---navigating the ArgoCD UI to create Applications, the GitHub UI to configure branch protection and webhooks, and \texttt{kubectl} to create RBAC, Sensors, and WorkflowTemplates. This manual process takes hours, is error-prone, non-reproducible, and impossible to version-control. XSDLC reduces it to a single declarative resource that can be reviewed in a pull request, stored in Git, and reconciled automatically.

% ----------------------------------------------------------------------------
\subsection{Quality Assurance Without Dedicated Testers}
\label{subsec:no-testers}
% ----------------------------------------------------------------------------

The most consequential operational finding is that DDT eliminates the need for dedicated API testers in the promotion pipeline. Because validation is embedded in the promotion flow---every promotion PR is automatically validated against the OpenAPI specification and, where configured, load-tested---the quality gate runs without human initiation, human authoring of test cases, or human interpretation of results.

API owners can trust that their production deployments have been validated against two contracts:

\begin{enumerate}
    \item \textbf{The specification contract} (P001--P005, P008, P009): The API's documentation, error handling, schema definitions, and security configuration match the declared specification. Any drift between the specification and the implementation is detected before promotion.
    \item \textbf{The performance contract} (load testing via \texttt{loadTestConfig}): The API handles the configured concurrent user count within the configured P95 latency threshold at the configured success rate. Any performance regression below these thresholds blocks promotion.
\end{enumerate}

This is not a claim that DDT replaces all testing. Unit tests, integration tests, and exploratory testing remain the developer's responsibility (the BYOCI boundary). DDT replaces the \textit{promotion-gate testing} that would otherwise require a dedicated QA engineer to run manual contract checks and load tests before each environment transition.

% ----------------------------------------------------------------------------
\subsection{Load Testing as Business Insurance}
\label{subsec:load-testing-insurance}
% ----------------------------------------------------------------------------

The quality gate does not prevent all production failures---it prevents \textit{preventable} failures. Consider the scenario where a production API experiences an out-of-memory (OOM) crash because the number of concurrent users has grown beyond the original capacity plan. The quality gate's response to this scenario is instructive:

The production environment \textit{was} load-tested before promotion. The \texttt{loadTestConfig} specified, for example, 50 concurrent users with a P95 latency threshold of 200\,ms and a 99.5\% success rate. The API passed these thresholds, and the promotion was approved. The OOM occurred because the \textit{actual} user load exceeded the \textit{configured} threshold---not because the system failed to test.

This is by design. The quality gate validates against \textit{declared business requirements}, not against unbounded traffic. The correct response to the OOM is not to blame the quality gate but to update the \texttt{loadTestConfig}---increase \texttt{concurrentUsers}, tighten \texttt{maxLatencyP95}, raise \texttt{minSuccessRate}---to reflect the new reality. Because these thresholds are fields in a Kubernetes custom resource, updating them is a one-line YAML change that takes effect on the next promotion cycle.

This pattern---declare thresholds, gate against them, update when reality diverges---transforms load testing from a periodic manual activity into a continuous, declarative contract. The thresholds are versioned in Git, reviewed in pull requests, and enforced automatically. When business requirements change, the thresholds change with them.

A natural extension (discussed in Section~\ref{sec:future-work}) is a controller that watches observability metrics (Prometheus, Grafana) and automatically adjusts \texttt{loadTestConfig} thresholds based on actual production traffic patterns. This would close the loop between observability and quality gating, making the system self-adjusting.

% ============================================================================
\section{CI/CD Integration Metrics}
\label{sec:cicd-metrics}
% ============================================================================

The following metrics characterize DriveBy's integration footprint:

\begin{table}[htbp]
\centering
\caption{CI/CD integration metrics for the DriveBy framework.}
\label{tab:cicd-metrics}
\small
\begin{tabular}{@{}lll@{}}
\toprule
\textbf{Metric} & \textbf{Value} & \textbf{Notes} \\
\midrule
CLI binary size          & \(\sim\)15\,MB         & Cross-compiled Go binary \\
Docker image size        & \(\sim\)25\,MB         & Distroless base image \\
Startup time             & \(<\)1\,s              & No JVM, no runtime dependencies \\
Validation time (strict) & 2--5\,s                & Depends on specification size \\
XSDLC provisioning time  & \(\sim\)15\,s          & From CR apply to all resources ready \\
End-to-end gate latency  & \(<\)90\,s             & Webhook to commit status posted \\
Resources per XSDLC CR   & \(\sim\)34             & For a 3-environment pipeline \\
Helm chart size          & \(<\)50\,KB            & OCI-distributed \\
\bottomrule
\end{tabular}
\end{table}

The sub-second startup time and 2--5 second validation time make DriveBy suitable for CI pipeline steps where latency directly impacts developer feedback loops. The 15-second XSDLC provisioning time compares favorably against the hours of manual configuration it replaces.

% ============================================================================
\section{Summary}
\label{sec:eval-summary}
% ============================================================================

The evaluation demonstrates three results:

\textbf{Detection accuracy} (RQ1). The controlled evaluation shows 100\% detection rate for injected defects with zero false positives. The defect injection protocol confirms that each principle checker detects its targeted defect class independently and deterministically.

\textbf{Automation extent} (RQ2). The large-scale evaluation reveals that real-world APIs systematically fail DDT's quality standards---even canonical examples like the Swagger Petstore. This validates DDT's thesis that specification quality is systematically neglected and that automated enforcement is necessary. The zero-test-authorship property is confirmed: all validation rules are derived from the specification without any manual test case writing. The agent-driven evaluation workflow further validates this property: the structured JSON reports were consumed directly by an AI agent for cross-API pattern analysis without human interpretation of individual results (Chapter~\ref{ch:ai-development}).

\textbf{GitOps integration} (RQ3). The operational evaluation demonstrates that XSDLC functions as a composable IDP feature, provisioning a complete quality-gated delivery pipeline from a single custom resource in 15 seconds. The system runs in production on Novelcore's cluster alongside existing platform infrastructure, validating the surgical coexistence model. Quality gate automation eliminates the need for dedicated testers in the promotion pipeline, and load testing thresholds serve as declarative, versionable business contracts.

Chapter~\ref{ch:ai-development} examines the AI-assisted development methodology that enabled this evaluation at scale. Chapter~\ref{ch:discussion} revisits the research questions with the combined evidence. Chapter~\ref{ch:conclusion} synthesizes the findings, discusses limitations, the ontological significance of XSDLC, and directions for future work.
