% Two-Tier Configuration Resolution — Chapter 5
% Shows: Helm defaults → XRD spec overrides (XSDLC model, no EnvironmentConfig)
\begin{tikzpicture}[
    >={Stealth[length=2.5mm]},
    node distance=1.2cm,
    tier/.style={
        rectangle,
        rounded corners=4pt,
        draw=#1!60!black,
        fill=#1!12,
        minimum width=4.5cm,
        minimum height=1.2cm,
        font=\small,
        align=center
    },
    desc/.style={
        font=\scriptsize,
        text=#1!70!black,
        align=left
    },
    result/.style={
        rectangle,
        rounded corners=5pt,
        draw=black!60,
        fill=black!8,
        minimum width=4.5cm,
        minimum height=0.8cm,
        font=\small\bfseries,
        align=center
    },
    override/.style={
        ->,
        thick,
        #1!50!black
    }
]

% Two tiers (top to bottom = lowest to highest priority)
\node[tier=blue] (helm) at (0,0) {\textbf{Tier 1: Helm Values}\\(values.yaml)};
\node[desc=blue] at (5.5,0) {Chart-wide defaults:\\domain, image, secrets, ports};

\node[tier=orange] (xrd) at (0,-2.2) {\textbf{Tier 2: XRD Spec}\\(per-resource)};
\node[desc=orange] at (5.5,-2.2) {Instance-level overrides:\\API config, validation mode, gates};

\node[result] (resolved) at (0,-4.0) {Resolved Configuration};

% Override arrows
\draw[override=blue] (helm) -- node[left,font=\scriptsize] {defaults} (xrd);
\draw[override=orange] (xrd) -- node[left,font=\scriptsize] {overrides} (resolved);

\end{tikzpicture}
