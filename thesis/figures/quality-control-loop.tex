% Quality Gate as Control Loop — Chapter 6, Section 6.6
% Control theory block diagram: Desired State → Controller → Actuator → Plant → Sensor → feedback
\begin{tikzpicture}[
    >={Stealth[length=2.5mm]},
    block/.style={
        rectangle,
        rounded corners=4pt,
        draw=#1!60!black,
        fill=#1!12,
        minimum width=3cm,
        minimum height=1cm,
        font=\small,
        align=center
    },
    signal/.style={
        font=\scriptsize,
        text=gray!70!black
    },
    sum/.style={
        circle,
        draw=gray!60,
        fill=gray!5,
        minimum size=0.5cm,
        font=\scriptsize
    }
]

% Desired state (setpoint)
\node[block=violet] (setpoint) at (0,0) {\textbf{Desired State}\\OpenAPI Spec};

% Sum point
\node[sum] (sum) at (2.4,0) {$\Sigma$};

% Controller
\node[block=teal] (controller) at (4.7,0) {\textbf{Controller}\\Argo Workflow\\DAG};

% Actuator
\node[block=orange] (actuator) at (7.7,0) {\textbf{Actuator}\\CommitStatus +\\Promoter};

% Plant
\node[block=blue] (plant) at (10.7,0) {\textbf{Plant}\\API Deployment\\(source env)};

% Sensor
\node[block=red!70!black] (sensor) at (7.7,-2.5) {\textbf{Sensor}\\DriveBy CLI\\(validation engine)};

% Forward path
\draw[->,thick,violet!50!black] (setpoint) -- (sum);
\draw[->,thick,gray!60] (sum) -- node[above,signal] {error} (controller);
\draw[->,thick,teal!50!black] (controller) -- node[above,signal] {pass/fail} (actuator);
\draw[->,thick,orange!50!black] (actuator) -- node[above,signal] {promote/block} (plant);

% Feedback path
\draw[->,thick,blue!50!black] (plant.south) -- ++(0,-1) -- node[above,signal] {runtime behavior} (sensor.east);
\draw[->,thick,red!50!black] (sensor.west) -| node[left,signal,pos=0.3,align=right] {validation\\report} (sum.south);

% Negative feedback annotation
\node[font=\tiny\bfseries,text=red!60!black] at (2.8,-1.2) {$-$};

% Labels for control theory mapping
\node[font=\tiny\itshape,text=violet!50!black,anchor=north] at (0,-0.7) {(specification)};
\node[font=\tiny\itshape,text=teal!50!black,anchor=north] at (4.7,-0.7) {(orchestrator)};
\node[font=\tiny\itshape,text=orange!50!black,anchor=north] at (7.7,-0.7) {(gate signal)};
\node[font=\tiny\itshape,text=blue!50!black,anchor=north] at (10.7,-0.7) {(deployment)};
\node[font=\tiny\itshape,text=red!50!black,anchor=north] at (7.7,-3.2) {(SDT validator)};

% Convergence annotation
\node[font=\scriptsize\itshape,text=gray!60,align=center] at (4.7,-3.8) {Closed-loop negative feedback:\\failure blocks promotion $\rightarrow$ developer fixes $\rightarrow$ re-validates $\rightarrow$ converges};

\end{tikzpicture}
